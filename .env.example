# Local development only. Do not commit real secrets.
# In production (Render), configure this in Environment Variables.
GEMINI_API_KEY=your_gemini_api_key_here
# PORT is optional locally; Render sets PORT automatically.
# PORT=3000
